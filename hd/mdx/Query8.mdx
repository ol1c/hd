-- 8. Jak liczba odwiedzin sal zmienia się w zależności od wieku eksponatów prezentowanych w danej sali?
SELECT
{ [Measures].[Visit Count] } ON COLUMNS,
NonEmpty(
[Room].[Number].Members * [Exhibit].[Ceratino Era].Members,
{ [Measures].[Visit Count] }
) ON ROWS
FROM [Museum DB 1]